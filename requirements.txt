PyQt5>=5.15.0
numpy>=1.21.0
Pillow>=8.3.0
pydicom>=2.3.0
scipy>=1.7.0
scikit-image>=0.18.0
opencv-python>=4.5.0
pylibjpeg>=2.0
pylibjpeg-libjpeg>=2.1
# gdcm은 설치가 복잡하므로 pylibjpeg로 대부분의 JPEG Lossless 처리 가능
# gdcm이 필요한 경우: conda create -n dicom_env python=3.9 && conda activate dicom_env && conda install -c conda-forge gdcm
